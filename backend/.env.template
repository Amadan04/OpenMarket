# server configuration
port=8080

# jwt secret key used for signing authentication tokens
# - replace this with a secure random string (at least 32 bytes / 64 hex chars)
# - do not commit your real secret to version control
# - changing this will invalidate all existing tokens
jwt_secret=change_jwt_secret
