import SwiftUI

struct ReviewsView: View {
    let sellerID: Int
    @State private var reviewsResponse: ReviewsResponse?

    var body: some View {
        // TODO: Replace with design handoff
        Text("ReviewsView — placeholder")
    }
}
